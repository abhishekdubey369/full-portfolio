let initialPath = "M 1000 20 Q 50 20 1000 20 ";

let finalPath = "M 1000 20 Q 50 20 1000 20 ";

export const mouseEnter = (dets,ref)=>{
    console.log(dets);
    console.log(ref.current);
}

export const mouseExit = (dets,ref) => {
    console.log(dets);
}

export const mouseMove = (dets,ref) => {
    console.log(dets);
}